document.addEventListener('DOMContentLoaded', function(){
    
    const options = {
        strings: [
            " Renan. "
        ],

        typeSpeed: 100,

        loop: false,

        startDelay: 1000,

        // backSpeed: 1500,

        // backDelay: 1500,
     
       showCursor: false
        
    };

    const typed = new Typed('#typing-effect', options);
});